{
    "name": "template",
    "version": "1.0.0",
    "description": "App boilerplate",
    "author": "Agora Binaria",
    "license": "ISC",
    "scripts": {
        "dev": "npm install && cd src/client && npm install && cd ../server && npm install && nodemon mean_template.js",
        "pre": "npm install && cd src/client && npm install && gulp build-dist && cd dist/server && npm install --production && node mean_template.js"
    },
    "devDependencies": {
        "nodemon": "^1.4.0",
        "del": "^1.2.0",
        "gulp": "^3.9.0",
        "gulp-if": "^1.2.5",
        "gulp-minify-html": "^1.0.4",
        "gulp-ng-annotate": "^1.1.0",
        "gulp-run": "^1.6.10",
        "gulp-uglify": "^1.2.0",
        "gulp-useref": "^1.2.0"
    }
}